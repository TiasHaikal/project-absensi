// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: [
    "http://localhost:3000",
    "https://527bdd5f84df.ngrok-free.app", // domain ngrok aktif
  ],
};

module.exports = nextConfig;
